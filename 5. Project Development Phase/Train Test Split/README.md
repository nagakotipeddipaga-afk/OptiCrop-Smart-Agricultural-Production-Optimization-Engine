Description:

The dataset is divided into feature variables (X) and target variable (y) for machine learning model training. The train_test_split() function from Scikit-learn is used to split the dataset into training and testing sets with appropriate test size and random state values. This process helps evaluate model performance and improves prediction reliability.

